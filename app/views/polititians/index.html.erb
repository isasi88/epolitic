<h1>Polititians/index</h1>

<div class="row">
	<div class="col-md-3">

		<%= link_to '<span class="glyphicon glyphicon-plus"></span> Añadir político'.html_safe, new_polititian_path, :class => "btn btn-primary btn-lg" %>

	</div>
	<div class="col-md-9">
		<p><%= params %></p>

		<ul class="list-group">		
			<% @polititians.each do |polititian| %>
				<li class="list-group-item">
				
					<div class="row">
					  <div class="col-md-3">
					    <a href="#" class="thumbnail">
					    	<%= image_tag("empty_profile.png", :alt => "") %>
						</a>
					  </div>
					  <div class="col-md-6">
					  	<h4>
					  		<%=link_to "#{polititian.first_name} #{polititian.last_name}", polititian_path(polititian)%>
					  	</h4>
					  	<p class="list-group-item-text">Text here<%= polititian.political_position %></p>
					  	<p><%= link_to("Delete", polititian_path(polititian), method: :delete, confirm: 'Are you sure?')%></p>
					  </div>
					</div>
				</li>
			<%end%>
		</ul>

	</div>
</div>


