<%= form_tag polititians_path, method: :get, :class => "", :role => "form" do %>

	<div class="form-group">
  <%= text_field_tag :term, params[:term], class: "form-control input-lg", placeholder: "Filtrar por nombre" %>
	</div>
		<div class="form-group">
			<%= collection_select :group_id_input, :group_id, Group.all, :id, :name, {include_blank: "Filtrar por partido político", selected: params[:group_id].to_i}, {:class => 'form-control', :name => "group_id" } %>
		</div>
		<div class="form-group">
			<%= collection_select :institution_id_input, :institution_id, Institution.all, :id, :name, {include_blank: "Filtrar por cita electoral", selected: params[:institutions_id].to_i}, { :class => 'form-control', :name => "institution_id" } %>
		</div>
  <%= submit_tag "Filtrar", name: nil, class: "btn btn-primary btn-lg" %>
  <%= link_to "Eliminar filtros", polititians_path %>
<% end %>