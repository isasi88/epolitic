<div class="row text-center">
	<div class="col-sm-3"></div>
	<div class="col-sm-6 box text-left">

		<h2 class="bottom-separation">Cambiar contraseña</h2>

		<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>

			<% if devise_error_messages!.present? %>
				<div class="alert alert-danger">
				  <%= devise_error_messages! %>
				</div>
			<% end %>
			
		  <%= f.hidden_field :reset_password_token %>

			<div class="form-group">
			  <div><%= f.label :password, "Nueva contraseña" %><br />
			    <%= f.password_field :password, autofocus: true, autocomplete: "off" %></div>
			</div>

			<div class="form-group">
			  <div><%= f.label :password_confirmation, "Confirmar nueva contraseña" %><br />
			    <%= f.password_field :password_confirmation, autocomplete: "off" %></div>
			</div>

			<div class="form-group">
			  <div><%= f.submit "Cambiar contraseña", class: "btn btn-primary" %> %></div>
		<% end %>
		</div>
		</br>
		<%= render "devise/shared/links" %>

	</div>	
</div>
