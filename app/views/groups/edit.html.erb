<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<% if @group.errors.any? %>
			<div class="alert alert-danger">
				<ul>
					<% @group.errors.full_messages.each do |error| %>
						<li><%= error %></li>
					<%end%>			
				</ul>
			</div>
		<%end%>
		<div class="box">
			<h1 class = "bottom-separation">Edit group</h1>
			<%= render 'form' %>
		</div>
	</div>
</div>	