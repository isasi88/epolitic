<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<% if @trial.errors.any? %>
			<div class="alert alert-danger">
				<ul>
					<% @trial.errors.full_messages.each do |error| %>
						<li><%= error %></li>
					<%end%>			
				</ul>
			</div>
		<%end%>
		<div class="box">
			<h1  class = "bottom-separation">Edit trial</h1>
			<%= render 'trial_form' %>
		</div>
	</div>
</div>	